defaults:
  - model: small_net
  - optimizer: adam
  - data: toy_dataset

# 训练超参数
train:
  epochs: 10
  batch_size: 32
  lr: 0.001
  print_every: 2

# 实验设置
experiment:
  name: baseline
  seed: 42
  device: cpu
  save_dir: ./outputs

# 日志设置
logging:
  level: INFO
  log_to_file: false