name: Windows payload diagnostics

on:
  push:
    branches: [codex/windows-payload-diagnostic]

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: windows-latest
    timeout-minutes: 18
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[test,lint]" pytest-github-actions-annotate-failures==0.4.2
      - name: Trace backend tests
        timeout-minutes: 12
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path test-results | Out-Null
          $env:PYTHONUNBUFFERED = '1'
          python -u -m pytest -vv -o faulthandler_timeout=60 --junitxml=test-results/pytest.xml 2>&1 | Tee-Object -FilePath test-results/pytest-live.log
          exit $LASTEXITCODE
      - name: Preserve test progress and thread stacks
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: windows-payload-diagnostic
          path: test-results/
          if-no-files-found: error
          retention-days: 14
